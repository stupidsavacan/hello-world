import { describe, expect, it, vi } from 'vitest';
import type { LoopDeckPack, Question } from '../src/core/models';
import { createQuestionImageAssetResolver } from '../src/packs/packAssetResolver';
import { mergeLoopDeckPacks } from '../src/packs/packMerger';
import { getQuestionPackId, resolveActivePacks } from '../src/packs/packResolver';
function imageQuestion(prompt:string,imageAsset='images/map.png'):Question{return{id:'q',moduleId:'m',type:'input',prompt,answer:'A',imageAsset};}function pack(packId:string,question:Question):LoopDeckPack{return{packVersion:1,packId,title:packId,folders:[{id:'f',title:'Folder'}],modules:[{id:'m',folderId:'f',title:'Module',subject:'demo',questionIds:[question.id]}],questions:[question]};}
describe('pack image asset resolution',()=>{it('tracks the pack that owns the active question after overrides',async()=>{const view=resolveActivePacks([pack('old-pack',imageQuestion('old')),pack('new-pack',imageQuestion('new'))]);const getPackAsset=vi.fn(async(packId:string,path:string)=>({assetId:`${packId}:${path}`,packId,path,mimeType:'image/png',dataUrl:'data:image/png;base64,iVBORw0KGgo='}));const resolver=createQuestionImageAssetResolver(view,{getPackAsset});expect(getQuestionPackId(view,'q')).toBe('new-pack');expect(await resolver(view.questionById.get('q')!)).toBe('data:image/png;base64,iVBORw0KGgo=');expect(getPackAsset).toHaveBeenCalledWith('new-pack','images/map.png');});it('keeps image paths when conflicting questions are renamed during merge',()=>{const merged=mergeLoopDeckPacks(pack('shared-pack',imageQuestion('old','images/old.png')),pack('shared-pack',imageQuestion('new','images/new.png'))).pack;expect(merged.questions.find(i=>i.id==='q')?.imageAsset).toBe('images/old.png');expect(merged.questions.find(i=>i.id==='q__merge_1')?.imageAsset).toBe('images/new.png');});});
