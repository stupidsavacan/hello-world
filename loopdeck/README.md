# LoopDeck application

This directory contains the LoopDeck learning application reconstructed incrementally from the supplied reference product. It is isolated as a self-contained application so existing repository functionality at the root can evolve independently.
